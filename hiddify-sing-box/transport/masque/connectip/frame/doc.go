// Package frame holds CONNECT-IP IPv4/TCP/UDP wire helpers (L3a frame layer).
//
// Target layout (W-IP-1): bootstrap and capsule policy move here from connectip root;
// pump/ and netstack/ remain sibling subpackages.
package frame
