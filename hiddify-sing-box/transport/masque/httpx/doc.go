// Package httpx defines the MASQUE HTTP overlay (H2/H3) abstraction used by coreSession.
// Phase A0: interfaces and test-hook fake layer. Phase D: fallback classifier (httpx/fallback.go).
package httpx
